.git
.venv
venv
__pycache__
*.pyc
*.pyo
.pytest_cache
.mypy_cache
.ruff_cache
.DS_Store
.env
.env.local
.vscode
.idea

# Runtime data lives on the Fly volume, not in the image
data/intake/
data/fio.db
data/fio.db-shm
data/fio.db-wal
data/vies_cache/

# Logs
*.log
nohup.out

# Backups & dev artefacts (ship-hardening gate, FIO retrospective 2026-05-20)
*.before-*
*.bak
*.orig
*~
.claude/
fio-bot-demo/
docs/
tests/
# Dev tooling — never invoked from inside the production container.
# Linters (lint_ru.py / lint_lv.py) and one-off scripts read docs/ at
# runtime on the developer machine / CI, not in the Flask app.
scripts/
# 2026-06-11 (Top-10 self-review) — but the backfill IS run on prod
# via `flyctl ssh console -C "python3 /app/scripts/backfill_legal_entity.py"`
# so it must ship in the image. Whitelist this one file.
!scripts/backfill_legal_entity.py
data/audit.jsonl
data/fx_rates_cache.json
data/registry_cache/
